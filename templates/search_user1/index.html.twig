{% extends 'base.html.twig' %}

{% block title %}Hello ShowUsersController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<form class="example-wrapper" role="search" method="post" action="{{ path('search') }}">
    <div>
        <input type="text" class="form-control" name="search" >
            <button type="submit" class="btn btn-success" name="sub">search1</button>
    </div>
    </form>
 
  

    <div class="example-wrapper">

    <table>
    <tr>
        <td> اسم الطالب </td>
        <td> الرقم الجامعي </td>
        <td> الكلية </td>
    </tr>
    {% for user in users %}
    <tr>
        <td> {{user.userName}} </td>
        <td> {{user.cardNumber}} </td>
        <td> {{user.college.collegeName}} </td>
        <td> 
        <a class="btn btn-primary" href="{{ path('user_edit', { id: user.id }) }}">تعديل</a>
        </td> 
    </tr>
     {% endfor %}
    </ul>
     </table>
</div>
{% endblock %}