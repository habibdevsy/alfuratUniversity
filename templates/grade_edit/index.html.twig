{% extends 'base.html.twig' %}
{% form_theme editform 'bootstrap_4_layout.html.twig' %}
{% block title %}تعديل درجة طالب{% endblock %}
{% block head %}

{% endblock %}

{% block body %}
 <style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    .foo code { color:red; }
</style> <br />
<div class="alert alert-dismissible alert-primary">
    <h2 style="text-align:center">تعديل درجة مادة</h2>
</div>
<br />
 {{ form_start(editform) }}
 
      {# {{ form_widget(editform.user, { "disabled":"false" }) }} #}
      {# {{ form_widget(editform.Subject, { "disabled":"disabled" }) }} #}
 {{ form_widget(editform) }}
 
 <br />
<button type="submit" class="btn btn-success">تعديل </button>
{{ form_end(editform) }}
{% endblock %}
